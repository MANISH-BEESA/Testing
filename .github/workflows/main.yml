# A name for your GitHub Action
name: Email Step Action

# This controls when the action runs
on:
  # Run on pushes to the 'main' branch
  push:
    branches: [ main ]
  # Allows you to run this manually from the Actions tab
  workflow_dispatch:

# A workflow is made of one or more jobs
jobs:
  test_job:
    # The type of machine to run the job on
    runs-on: ubuntu-latest
    steps:
      # Step 1: Check out the code from your repository
      - name: Check out repository code
        uses: actions/checkout@v4

      # Step 2: The required step with your email in the name
      - name: Step run by 23f3003694@ds.study.iitm.ac.in
        run: echo "This step ran successfully!"
